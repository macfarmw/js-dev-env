# js-dev-env

# Software
git scm  
node js  
Visual Studio Code  

# npm global installs needed
```
npm install -g nsp
npm install -g localtunnel
```
To launch localtunnel from the terminal:  
PS C:\...\lt --port 3000
